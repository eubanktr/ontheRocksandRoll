function getInput(event) {
    event.preventDefault()
    // assign variable to user input
    // add fetch function to variable that stores input
    // if fetch response is ok, continue with the fetch function and replace document with results.html
    // if it fails, return and show error message with modal or whatever you want
    // add event listener to designated button on click
}